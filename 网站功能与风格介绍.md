# 猫驼旅者客栈网站功能与风格介绍（精简版）

## 一、简要技术栈

- 前端：Vue 3、Vue Router、Element Plus、Axios、Vite
- 后端：Node.js、Express
- 数据库：MySQL
- 文件上传：Multer

---

## 二、网站功能

### 1. 访客端

- **首页**：品牌展示、轮播图、客栈简介、特色活动入口。
- **活动页**：时间线浏览活动，支持时间定位和跳转详情。
- **活动详情**：查看完整活动信息，支持图片预览。
- **人员页**：展示人员姓名、简介、联系方式和照片。
- **物品页**：展示物品名称、时间、介绍和图片。

### 2. 管理端

- **登录/退出**：管理员通过账号密码和验证码进入后台。
- **活动管理**：新增、编辑、删除活动，支持上传活动图片。
- **人员管理**：新增、编辑、删除人员，支持上传人员图片。
- **物品管理**：新增、编辑、删除物品，支持上传物品图片。

### 3. 整体体验

- 顶部导航支持快速切换页面。
- 页面结构统一，浏览与维护路径清晰。
- 支持不同屏幕尺寸下的正常浏览。

---

## 三、网站风格

- **整体调性**：温暖、亲和、轻文艺、带生活感与故事感。
- **色彩风格**：暖棕与米白为主，视觉柔和，阅读舒适。
- **布局风格**：卡片化 + 图文结合，信息清楚不杂乱。
- **交互风格**：按钮与卡片有轻量反馈，操作直观。
- **响应式表现**：大屏更饱满，小屏更紧凑，风格保持一致。

---

## 四、一句话总结

这是一个以“内容展示 + 后台维护”为核心的网站：前台负责讲好客栈故事，后台负责高效更新内容，整体风格温暖统一、易看易用。
# 猫驼旅者客栈网站功能与风格介绍（精简版）

## 一、网站功能

### 1. 访客端

- **首页**：品牌展示、轮播图、客栈简介、特色活动入口。
- **活动页**：时间线浏览活动，支持按时间定位与跳转详情。
- **活动详情**：查看活动完整信息与图片预览。
- **人员页**：展示人员姓名、简介、联系方式和照片。
- **物品页**：展示活动物品名称、介绍、时间与图片。

### 2. 管理端

- **登录/退出**：管理员通过账号密码和验证码进入后台。
- **活动管理**：新增、编辑、删除活动，支持上传活动图片。
- **人员管理**：新增、编辑、删除人员，支持上传人员图片。
- **物品管理**：新增、编辑、删除物品，支持上传物品图片。

### 3. 整体体验

- 顶部导航支持快速切换页面。
- 页面结构统一，浏览与维护路径清晰。
- 支持不同屏幕尺寸下的正常浏览。

---

## 二、网站风格

- **整体调性**：温暖、亲和、轻文艺、带生活感与故事感。
- **色彩风格**：暖棕与米白为主，视觉柔和，阅读舒适。
- **布局风格**：卡片化 + 图文结合，信息清楚不杂乱。
- **交互风格**：按钮与卡片有轻量反馈，操作直观。
- **响应式表现**：大屏更饱满，小屏更紧凑，风格保持一致。

---

## 三、一句话总结

这是一个以“内容展示 + 后台维护”为核心的网站：前台负责讲好客栈故事，后台负责高效更新内容，整体风格温暖统一、易看易用。
# 猫驼旅者客栈网站功能与风格介绍

本文档仅介绍网站的**功能设计**与**视觉风格设计**，不包含技术栈与开发实现说明。

---

## 一、网站定位

网站面向“客栈品牌展示 + 内容运营 + 后台维护”场景，服务两类用户：

- **访客用户**：浏览客栈信息、活动故事、人员介绍、物品展示。
- **管理员用户**：登录后维护活动、人员、物品与图片内容。

---

## 二、功能介绍（完整清单）

## 1) 访客端功能

### 1.1 首页展示

- 展示品牌标识、主视觉轮播图与客栈简介。
- 展示特色活动卡片，突出重点内容。
- 支持从首页推荐活动快速跳转到活动详情页。
- 保持首屏信息集中，便于新访客快速理解网站主题。

### 1.2 活动记录页

- 以时间线形式展示活动内容，增强“经历感”和“故事感”。
- 左侧提供时间定位导航，支持快速滚动定位到目标活动。
- 每条活动可展示标题、时间、参与信息、摘要描述和图片。
- 点击活动条目后可进入详情页查看完整内容。

### 1.3 活动详情页

- 展示单个活动的完整信息：标题、时间、参与人员、详细描述。
- 支持多图展示。
- 支持单图点击放大预览，便于查看活动细节。
- 提供返回入口，减少浏览路径断裂。

### 1.4 人员介绍页

- 以卡片方式展示人员信息。
- 每个条目可包含姓名、简介、联系方式、配图。
- 页面布局强调快速扫读和人物辨识度。

### 1.5 活动物品页

- 展示活动相关物品清单。
- 每个物品可包含名称、时间、介绍文字、图片。
- 采用网格化展示，便于批量浏览。

### 1.6 全站基础浏览能力

- 顶部导航支持主页、活动、人员、物品页面切换。
- 页面结构统一（头部/主体/页脚），降低使用成本。
- 支持桌面与中小屏设备浏览，信息层级保持清晰。

---

## 2) 管理端功能

### 2.1 管理员登录与退出

- 管理员通过账号、密码、验证码登录后台。
- 登录成功后可进入管理页面并进行内容维护。
- 支持退出登录，退出后恢复访客可见范围。

### 2.2 活动管理

- **新增活动**：录入标题、时间、参与信息、描述、图片。
- **编辑活动**：对已有活动信息进行修改更新。
- **删除活动**：移除不再展示的活动内容。
- **图片上传**：可上传图片并回填到活动记录中。

### 2.3 人员管理

- **新增人员**：录入姓名、简介、联系方式、图片等内容。
- **编辑人员**：更新人物介绍信息。
- **删除人员**：清理无效或过期人员信息。
- **图片上传**：支持人员照片上传与即时展示。

### 2.4 物品管理

- **新增物品**：填写名称、时间、介绍、图片。
- **编辑物品**：更新物品展示内容。
- **删除物品**：移除不再展示的物品。
- **图片上传**：支持物品图上传并即时用于页面展示。

### 2.5 后台操作体验

- 采用分区管理（活动/人员/物品），定位清晰。
- 列表与表单结合，支持“查看-编辑-保存”连续操作。
- 删除操作带确认，降低误删概率。
- 上传后可快速看到结果，减少维护等待成本。

---

## 三、功能完整性检查清单（验收用）

### 3.1 浏览链路

- 首页可正常加载主视觉与推荐内容。
- 导航可正常跳转到活动、人员、物品页面。
- 活动列表可进入详情并可返回。
- 人员页和物品页图文信息显示正常。

### 3.2 管理链路

- 登录成功后可进入管理页面。
- 活动可新增、编辑、删除。
- 人员可新增、编辑、删除。
- 物品可新增、编辑、删除。
- 图片上传后可在对应页面正常显示。
- 退出登录后管理能力失效。

### 3.3 使用体验链路

- 页面切换自然，视觉层级稳定。
- 文本与图片内容可读性良好。
- 关键按钮、卡片、导航有明确交互反馈。

---

## 四、风格介绍（视觉与交互）

## 1) 风格关键词

- 温暖
- 亲和
- 生活感
- 手作感
- 轻文艺
- 故事感

## 2) 色彩风格

- 采用暖棕、土黄色、浅木色为主调。
- 通过柔和背景和低刺激对比，营造舒适浏览氛围。
- 强调色用于标题、导航焦点与关键按钮，形成视觉引导。

## 3) 页面气质

- 整体偏“民宿与旅居叙事”风格，而非商务后台风格。
- 内容呈现强调情绪与故事，而不只是信息堆叠。
- 图片承担主要情绪表达，文本负责补充细节与上下文。

## 4) 布局风格

- 以卡片化布局为主，信息边界清晰。
- 首页采用“视觉主图 + 简介 + 推荐内容”结构，首屏表达直接。
- 活动页采用时间线结构，强化内容时间顺序。
- 人员/物品页采用卡片与网格组合，兼顾整齐与效率。

## 5) 组件细节

- 大量使用圆角、浅边框、柔和阴影，体现温和质感。
- 图片统一裁切比例与展示尺寸，保证整体规整度。
- 标题与正文层级分明，提升扫读效率。

## 6) 交互风格

- 卡片 hover 有轻微位移与阴影变化，增强可点感。
- 按钮交互明确，点击结果反馈及时。
- 操作流程尽量短路径，减少用户重复动作。

## 7) 响应式表现

- 在大屏下尽量利用可视宽度，内容更饱满。
- 在中小屏下自动调整为更易读的纵向结构。
- 保持不同屏幕下风格一致、信息不丢失。

---

## 五、功能与风格的一致性

网站在“看起来舒服”和“用起来高效”之间保持平衡：

- 前台通过统一风格承载品牌与活动故事，增强记忆点。
- 后台通过结构化管理能力保证内容持续更新。
- 视觉风格服务于内容表达，功能设计服务于运营效率。

---

## 六、可继续增强方向（仅功能与风格）

- 增加活动筛选、搜索与归档功能，提升可查找性。
- 增加首页“最新动态/本月活动”模块，强化更新感。
- 人员页加入标签分类，增强识别度。
- 物品页加入分类筛选，降低浏览负担。
- 进一步统一品牌图片风格和文案语气，提升整体辨识度。
# 猫驼旅者客栈网站功能与风格介绍

## 1. 项目定位与目标

`WebForHostel` 是一个前后端分离的客栈展示与管理系统，目标是把“品牌展示 + 活动记录 + 人员介绍 + 物品管理 + 管理后台”统一在一个可维护的网页应用中。  
项目面向两类使用者：

- 普通访客：浏览主页、活动、人员、物品等内容。
- 管理员：登录后台后对活动、人员、物品进行新增、编辑、删除，以及上传图片资源。

---

## 2. 系统架构概览

### 2.1 前端

- 技术栈：`Vue 3` + `Vue Router` + `Element Plus` + `Axios` + `Vite`
- 目录重点：
  - `src/main.js`：应用入口，注册路由与 UI 组件库。
  - `src/router/index.js`：路由定义（主页、活动、详情、人员、物品、登录、管理）。
  - `src/views/*`：页面层。
  - `src/services/*`：接口层与认证层。

### 2.2 后端

- 技术栈：`Node.js` + `Express` + `MySQL` + `Multer`
- 目录重点：
  - `src/server.js`：REST API、上传、认证与鉴权中间件。
  - `src/db.js`：数据库初始化（建库、建表）。
  - `src/seed.js`：启动时初始化管理员与示例业务数据。

### 2.3 数据流

1. 前端页面通过 `services/api` 发起请求。
2. `http.js` 统一注入 `Bearer token`（若管理员已登录）。
3. 后端通过 `requireAdmin` 保护写接口（新增/更新/删除/上传）。
4. 后端持久化到 MySQL，并返回结构化 JSON 给前端渲染。

---

## 3. 核心功能清单（按业务模块）

## 3.1 公共浏览功能（访客可访问）

### A. 首页展示（`/`）

- 顶部轮播图展示客栈视觉内容。
- 品牌标题、简要介绍文案、温馨风格信息块。
- 特色活动推荐区，支持点击跳转活动详情页。

### B. 活动记录列表（`/activities`）

- 左侧“时间定位”导航，按活动时间快速定位。
- 右侧时间线卡片展示活动标题、参与信息、描述和主图。
- 点击卡片可进入详情页。

### C. 活动详情（`/activity/:id`）

- 展示活动时间、参与人员、详细描述。
- 支持多图展示与点击预览（图片查看器）。

### D. 人员介绍（`/people`）

- 卡片式展示人员姓名、简介、联系方式。
- 支持人员图片展示。
- 保持统一品牌配色与动效反馈。

### E. 活动物品展示（`/items`）

- 网格化卡片展示物品名称、时间、介绍与图片。
- 支持响应式布局，适配不同屏宽。

## 3.2 管理端功能（需管理员登录）

### A. 登录系统（`/login`）

- 账号密码 + 验证码输入流程。
- 登录请求走后端 `/api/auth/login`，前端不再保存硬编码账号密码。
- 登录成功后获取 token 并写入本地存储。

### B. 管理员身份校验

- 页面进入后台前调用 `/api/auth/me` 验证 token。
- token 无效会自动清理并跳转登录。
- 退出登录时调用 `/api/auth/logout` 销毁会话。

### C. 活动管理

- 新增活动：标题、时间、人员、描述、图片。
- 编辑活动：回填并更新。
- 删除活动：同步删除记录（并尝试清理上传文件引用）。

### D. 人员管理

- 新增/编辑/删除人员信息（姓名、性格、技能、联系方式、照片）。
- 支持图片上传并自动写入可访问路径。

### E. 物品管理

- 新增/编辑/删除物品（名称、时间、描述、图片）。
- 支持图片上传并写入资源路径。

### F. 上传管理

- 统一上传接口 `/api/upload`（受鉴权保护）。
- 上传后返回 `/uploads/...` 路径供页面直接展示。

---

## 4. 安全与权限机制（当前实现）

## 4.1 唯一管理员策略

- 系统启动初始化时会重置管理员数据，仅保留唯一管理员：
  - 用户名：`rovin`
  - 密码：`109901`（数据库存储为 SHA-256 哈希）

## 4.2 鉴权模型

- 后端维护 `admin_sessions` 会话表。
- 登录后签发 token，后续请求以 `Authorization: Bearer <token>` 传输。
- 所有写操作接口要求管理员身份：
  - `POST/PUT/DELETE /api/activities`
  - `POST/PUT/DELETE /api/people`
  - `POST/PUT/DELETE /api/items`
  - `POST /api/upload`

## 4.3 错误处理策略

- 未登录或会话失效返回 `401 Unauthorized`。
- 前端收到 `401` 自动清 token，避免伪登录状态残留。

---

## 5. 界面风格与视觉语言

## 5.1 风格关键词

- 温暖、手作、民宿氛围、轻文艺、可读性优先

## 5.2 视觉特征

- 主色调偏暖棕、暖黄，贴合客栈与木质手作氛围。
- 大量使用圆角卡片、柔和阴影、细边框，营造亲和感。
- 图文并列与时间线布局增强“故事感”与“活动轨迹感”。
- 关键交互（卡片 hover、按钮）提供轻量动效反馈。

## 5.3 排版与信息层级

- 首页：先视觉吸引（轮播）再内容引导（品牌与特色活动）。
- 列表页：先结构导航（时间定位）再卡片内容，减少认知负担。
- 详情页：文本信息与图片分栏，移动端自动纵向堆叠。

## 5.4 响应式策略

- 使用 `el-row/el-col` 栅格系统适配不同屏幕尺寸。
- 在中小屏自动切换为纵向布局，保证可读性与触控友好。

---

## 6. API 能力总览

## 6.1 认证接口

- `POST /api/auth/login`：管理员登录，返回 token
- `GET /api/auth/me`：校验当前会话
- `POST /api/auth/logout`：退出并销毁会话

## 6.2 业务接口

- 活动：`GET /api/activities`、`POST`、`PUT /:id`、`DELETE /:id`
- 人员：`GET /api/people`、`POST`、`PUT /:id`、`DELETE /:id`
- 物品：`GET /api/items`、`GET /api/items/:id`、`POST`、`PUT /:id`、`DELETE /:id`
- 上传：`POST /api/upload`
- 健康：`GET /api/health`

---

## 7. 数据模型概览

- `activities`：活动标题、时间、参与信息、描述、照片数组
- `people`：姓名、性格、技能、联系方式、照片数组
- `items`：物品名称、时间、描述、单图
- `admins`：管理员账号与密码哈希
- `admin_sessions`：管理员会话 token 与过期时间

---

## 8. 完整性检查结果（本次）

- 前端构建通过：`npm run build` 成功。
- 后端语法检查通过：`node --check src/server.js` 成功。
- 核心路径完整：
  - 路由定义与页面组件齐全。
  - 服务层与后端接口对齐。
  - 鉴权链路已覆盖管理写接口。

---

## 9. 当前可继续优化方向（后续迭代建议）

- 管理员密码改为环境变量注入 + 首次启动引导创建，降低硬编码运维风险。
- 为后端增加统一异常处理中间件与接口级日志。
- 增加自动化测试（接口鉴权、关键 CRUD、登录流程）。
- 前端继续拆分 `element-plus` 体积（按需引入）以进一步优化首屏加载。

---

## 10. 使用说明（管理员）

1. 启动后端与前端服务。
2. 打开登录页，使用管理员账号：
   - 用户名：`rovin`
   - 密码：`109901`
3. 登录后进入后台，执行活动/人员/物品管理与图片上传。

> 说明：管理员账号由后端初始化逻辑统一维护；若重启后端并触发初始化，会重置为上述唯一账号。

